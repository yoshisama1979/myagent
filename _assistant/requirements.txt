# 経営サポート用業務パートナー (_assistant) の Python 依存
# インストール: python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt

# Claude API クライアント
anthropic>=0.40

# Web UI（FastAPI + Jinja2 + cookie session）
# Chainlit は Python 3.14 非対応のため不採用。Python 追従が確実な FastAPI 構成を採る。
fastapi>=0.115
uvicorn[standard]>=0.32
jinja2>=3.1
itsdangerous>=2.2
python-multipart>=0.0.12

# HTTP クライアント（hana-tools への接続）
httpx>=0.28.1

# OS の証明書ストアを使う（社内 TLS / プロキシ証明書対応）
truststore>=0.10.4

# 開発・テスト
pytest>=8.0
