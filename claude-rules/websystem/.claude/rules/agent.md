---
description:
globs:
alwaysApply: false
---

# エージェント向けルール

## 必ず最初に読むこと

- **このファイル（rules/agent.md）を最初に読み込んでから作業を開始すること。**
- プロジェクト実装時は、**作業のフェーズに該当するルールファイルを確認**したうえで作業すること（全件先読みしない＝CLAUDE.md の方針）。

## モード判定（最優先）

作業に入る前に、対象タスクのゴール記録ファイル（`documents/goals/<案件ID>-<機能>.md`）の有無と内容、およびユーザーの発話意図を確認すること。

| 状態・トリガー | 適用モード | 参照ファイル |
|--------------|-----------|-------------|
| ゴール記録ファイル無し、かつユーザーが **仕様策定・自走・まとまった実装** を示した（「仕様を考えたい」「ヒアリングお願い」「自走で作って」等） | **Dialogue モード**（仕様策定） | [`rules/dialogue-mode.md`](./dialogue-mode.md) |
| ゴール記録ファイル無し、かつ **小修正・単発質問・通常実装**（typo 修正・1関数の挙動確認・既存パターンの単純踏襲 等） | **都度承認モード** | 本ファイル |
| 存在し、`mode: autopilot` かつ `status: dialogue` | **Dialogue モード**（仕様策定継続中） | [`rules/dialogue-mode.md`](./dialogue-mode.md) |
| 存在し、`mode: autopilot` かつ `status: active` | **Autopilot モード**（自走中） | [`rules/autopilot.md`](./autopilot.md) |
| 存在し、`mode: autopilot` かつ `status: paused` | 停止理由に応じて分岐（後述「補足」参照） | [`rules/autopilot.md`](./autopilot.md) |
| 存在し、`mode: autopilot` かつ `status: completed` | **完了済み**。原則作業しない。追加要望ならユーザーに確認のうえ新規 goals.md 作成、または `status` を `dialogue` に戻して再仕様策定 | — |
| 存在し、`mode: standard` または `mode` 未指定 | **都度承認モード** | 本ファイル |

### 補足

- **Dialogue モードと Autopilot モードはペア**で動く（合意なき自走を防ぐため）。Autopilot モードは Dialogue モードを通過した goals.md がある前提で起動する
- **Inbox モード**（`inbox-mode.md`）は上記モード判定とは独立した **並走型モード**。`/inbox` スラッシュコマンドで起動し、別セッションで `documents/inbox.md` に「次やってほしいこと」を追記するだけ。開発エージェントは Autopilot 達成報告時に inbox を確認する
- いずれのモードでも、本ファイル以降に列挙する各ルールファイル（`dev.md` / `tdd.md` / `refactoring.md` 等）には共通して従う。**違いは「承認の頻度」と「対話の有無」だけ**
- **Autopilot モード移行は明示的な合言葉のみ**で実行。「自走で実装して」「Autopilot で進めて」「自走お願い」等、**実装開始と自走許可が両方読める発話** に限定する。「進めて」「OK」だけでは次質問への同意との区別がつかないので、AI 側で「Autopilot 起動でよろしいですか？」と確認を挟む
- 移行時は goals.md の `status` を `dialogue` → `active` に更新してから自走に入る
- `status: paused` からの分岐：
  - **判断待ち（クリティカル判断・安全装置・STOP.md）** → ユーザー確認後 `active` へ戻して自走再開
  - **仕様変更・スコープ変更が必要** → `status: dialogue` へ戻し Dialogue モードへ
  - 判断に迷う場合はユーザーに確認

## 仕様書の場所

- 本プロジェクトの仕様書は **documents/specs/ 配下（HTML）** に記載されている（`rules/spec-format.md` 準拠）。
- 機能要件・画面仕様・データ仕様などを確認する際は、必ず documents/specs/ の該当ページを参照すること。
- **どれを正とするか**（全ルール共通の線引き）：
  - **新規プロジェクト**：`documents/specs/` のみを使う（`document.md` は作らない）。
  - **既存案件で `documents/document.md` しか無い**（`project-config.md` にその旨が明記されている）：暫定で `document.md` を正とする。
  - **両方ある移行期**：`documents/specs/`（HTML）側を正とする。

---

## ルールディレクトリ内の各ファイルの役割（概要）

> ※この表は要約。**説明が各ルール本体・CLAUDE.md の表と食い違う場合は本体が正**。

| ファイル                   | 役割・概要                                                                                                                                                                                            |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **agent.md**（本ファイル） | エージェントの入口。最初に読むこと、仕様書の場所、および本一覧を定める。                                                                                                                              |
| **refactoring.md**         | リファクタリング実施ガイドライン。挙動を変えず可読性・保守性を上げるための原則、Git Diff ベースの STRICT スコープ、実施する／しない判断基準、手順、Laravel/React 等の言語別ルール、報告フォーマット。 |
| **tdd.md**                 | TDD/BDD 開発インストラクション。確認優先・一歩ずつ・推測禁止の原則、調査→BDDシナリオ→テスト設計→RGBC（Red/Green/Blue/Commit）のフェーズ、各段階でのユーザー承認、フェーズ終了時の報告ルール。         |
| **testcode.md**            | 自動テストの実装ルール。`SafeTestCase` 継承必須（本番DB保護）。SafeTestCase 配下なら RefreshDatabase / DatabaseTransactions 使用可。相対値アサーション、テストDBの使い方と TDD サイクル。 |
| **create-test.md**         | テスト仕様書の作成ルール。CSV 形式でテスト仕様書を書くためのフォーマット（11列）、大項目〜小項目の階層、手順・動作結果・備考の記述ルールと記述例。                                                    |
| **spec-format.md**         | 仕様書（`documents/specs/` 配下のHTML）の作成・更新ルール。配置場所・命名・Tailwind CSS の使い方・ページ構造・目次運用・段階的移行方針。                                                              |
| **estimation.md**          | 見積もり用タスク抽出・難易度評価ルール。新機能を区分（バックエンド/フロント/既存機能波及/テスト/ドキュメント）で洗い出し、リスク（影響度）・難易度・工数の3軸を10段階で評価する。出力形式（CSV/HTML）。 |
| **dialogue-mode.md**       | **Dialogue モード（仕様策定）**。Autopilot モードとペアで動く前段。ユーザーと対話して goals.md の「確定仕様」を固める。意見・推奨を積極的に提示、派生アイデアは `<案件ID>-ideas.md` に深堀せず残す。完了判定を通過したら Autopilot モードへ移行。 |
| **autopilot.md**           | **Autopilot モード（自走）**。Dialogue モードを通過した goals.md を「正」として、完成条件達成まで自走。クリティカル判断（破壊的操作・外部送信・本番デプロイ・仕様根本変更等）でのみ一時停止。コミットは自走で作成、push は手動。 |
| **inbox-mode.md**          | **Inbox モード（並走型）**。`/inbox` スラッシュコマンドから起動する別セッションの軽量モード。開発エージェント稼働中の裏で、ユーザーの「次やってほしいこと」を `documents/inbox.md` に追記する。実装・仕様策定には踏み込まず、追記専門。 |
| **ui-design.md**           | UI/デザインの実装・変更ルール。`Design.md`=デザインSSOT の扱い方、UI実装原則（洗練・一貫性・UX・フォーム・レスポンシブ）、アクセシビリティ最低ライン、Do/Don't ガード。 |
| **design-replication.md**  | デザインカンプ／モックアップ（特にAI生成画像）からUIを再現するときの周回。推測を実測に変え、実ブラウザ描画（スクショ）で照合する。 |
| **ui-check.md**            | UI実装完了後の目視品質ゲート（動作・アクセシビリティ・レスポンシブ・一貫性・画像・リンク）。実装フロー Step 8／Autopilot 達成判定の前に通す。自動テスト（frontend-test.md）とは別物。 |

### フロントエンドテスト
- フロントエンドの自動テスト作成時のルールは **rules/frontend-test.md** を参照すること（三者整合性チェック必須）。

- フロントエンドの自動テスト（Vitest + React Testing Library）の使い方・書き方は **next/src/\_\_tests\_\_/README.md** を参照すること。

---

## 大きめの機能を実装する前に（設計フェーズ）

複数テーブル変更・新画面・新エンドポイントが絡む機能、もしくは
「どう作るか」が一意に決まらない機能では、**Step 1（仕様の記載）に入る前に
設計フェーズを必ず通す**こと。

### 設計フェーズで決めること

1. **目的とポリシー**: その機能で何を解決したいか、誰がどう使うかをユーザーと合意
2. **技術的選択肢**: 主要な設計判断（ストレージ・ライブラリ・データ構造・
   権限モデル等）を選択肢として整理し、トレードオフを並列提示
3. **ロードマップ**: 実装を「1コミット = 1論点」で完結できるよう、
   Step 1〜N に分解する

### 進め方

- ユーザーの要望に対し、技術的観点で**最適と思う案を推奨**したうえで複数選択肢を提示する
- ユーザーの最初の指示をそのまま鵜呑みにせず、より良い方法があれば質問・提案する
- 主要決定が揃ったら、**規模に応じた段階分け**でロードマップを作りユーザー承認を得る。
  - ステップ数は固定ではない。1 ステップで済む小変更もあれば、複数ステップに分けるべき変更もある
  - 段階分けの目的は**「一気に大きな変更を進めないためのストッパー」**。
    その目的が満たされる粒度で、その都度規模を見て判断する
  - ここでいう「1 ステップ = 1 コミット = 1 論点」の粒度は、TDD の Red/Green/Refactor の
    内部サイクル（rules/tdd.md）とは別レイヤー。各ステップの中で TDD を回しても矛盾しない
- 設計フェーズが終わってから Step 1 の仕様書記載に着手する

---

## 一つの機能の実装フロー（進め方）

**重要**: 一つ一つのステップが完了するごとに、必ずユーザーにフェーズを報告し、次のステップへ進むか確認すること。

| 順  | ステップ                               | 実施内容                                                                                                                             | 完了時のアクション                                           |
| --- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ |
| 1   | **仕様の記載**                         | ユーザーが「何を作りたいか」を伝える → その内容をもとに **`documents/specs/` 配下の該当HTMLページ**（`rules/spec-format.md` 準拠）に仕様を書き込む。新規ページは `documents/specs/_template.html` をコピーし、`documents/specs/index.html` の目次にもリンクを追加する。 | 仕様記載完了を報告し、**ユーザーにフェーズを移す**。         |
| 2   | **バックエンド実装（TDD）**            | 記載した仕様を基に、**バックエンドから TDD** で実装する。rules/tdd.md および rules/testcode.md に従う。                              | 実装完了を報告し、**ユーザーにフェーズを移す**。             |
| 3   | **コミット準備（バックエンド）**       | ユーザーがファイルをステージングする。**ステージングされたファイルの差分**を確認し、問題がなければ**コミットメッセージ**を提案する。 | 提案完了を報告し、**ユーザーにフェーズを移す**。             |
| 4   | **リファクタリング（バックエンド）**   | **rules/refactoring.md** を確認し、それに従って**ステージングされたファイル**（バックエンド）に対してリファクタリングを行う。        | リファクタリング完了を報告し、**ユーザーにフェーズを移す**。 |
| 5   | **フロントエンド実装（TDD）**          | フロントエンドの実装を行う。rules/tdd.md および rules/frontend-test.md に従い、**自動テスト作成 → 実装 → テスト仕様書(test.csv)更新** をセットで行う。UI/デザインに触れる場合は rules/ui-design.md（カンプ再現時は rules/design-replication.md）を併用する。 | 実装完了を報告し、**ユーザーにフェーズを移す**。             |
| 6   | **コミット準備（フロントエンド）**     | ユーザーがファイルをステージングする。**ステージングされたファイルの差分**を確認し、問題がなければ**コミットメッセージ**を提案する。 | 提案完了を報告し、**ユーザーにフェーズを移す**。             |
| 7   | **リファクタリング（フロントエンド）** | **rules/refactoring.md** に従って、**ステージングされたファイル**（フロントエンド）に対してリファクタリングを行う。                  | リファクタリング完了を報告し、**ユーザーにフェーズを移す**。 |
| 8   | **動作確認・仕様書更新**               | UI変更を含む場合は、まず **rules/ui-check.md** の目視品質ゲートを通して結果を報告する。ユーザーが動作を確認する。その後、**`documents/specs/` 配下の該当HTMLページを再度更新**する（必要なら index.html の目次・関連ページ間のリンクも更新）。 | 更新完了を報告し、**ユーザーにフェーズを移す**。             |
| 9   | **テスト仕様書の作成**                 | 更新した仕様に対する**テスト仕様書**を **documents/test.csv** に作成する。rules/create-test.md に従う。                              | 作成完了を報告し、**ユーザーにフェーズを移す**。             |

以上で**一連の一つの機能の実装は完了**とする。次の機能に進む場合はステップ1から繰り返す。

---

## 機能実装時にAIがすべきこと（共通原則）

フローに加え、以下を守ること。

### 実装前

- **仕様の確認**: `documents/specs/` 配下の該当HTMLページ（旧仕様は `documents/document.md`）およびユーザーの指示を読み、実装範囲と受け入れ条件を把握する。
- **曖昧さの解消**: 仕様や要件が不明な点があれば、**実装せずに質問として列挙**し、ユーザーの回答を得てから進める。
- **既存コードの把握**: 既存のディレクトリ構成・命名規則・コーディング規約を確認し、それに合わせて実装する。
- **技術アドバイザーとして関わる**: ユーザーの意見をそのまま実装するのではなく、技術的に最適な選択肢があれば**推奨案として提示**し、トレードオフを並べて質問する。ユーザーの判断材料となる情報を揃えるのが AI 側の責務。

### 実装中

- **仕様の範囲内で実装**: 仕様書に書かれていない機能や仕様の拡張は、ユーザーが明示的に依頼するまで行わない。
- **推測禁止**: 判断が分かれる点・解釈が複数ある点は、推測で実装せず質問する（rules/tdd.md に準拠）。
- **テストの実行確認**: バックエンド・フロントエンドの実装後、該当するテストを実行し、**成功していることを確認**してからフェーズを移す。
- **エラー・境界の考慮**: 仕様に異常系・バリデーション・境界値が含まれる場合は、それらを実装とテストに反映する。

### セキュリティ・環境

- **秘密情報の扱い**: APIキー・パスワード・トークン等をソースコードにハードコードしない。環境変数や設定ファイルで管理し、必要に応じて .env.example に項目のみ記載する。
- **依存関係**: 新規パッケージ・ライブラリを追加する必要がある場合は、理由と用途を簡潔に説明する。

### コミット準備時（差分確認）

- **問題がある場合**: ステージングされた差分に、明らかなバグ・仕様違反・セキュリティリスク・不要な変更が含まれる場合は、**コミットメッセージを提案せず**、問題点を指摘してユーザーに報告する。
- **問題がない場合**: 差分が仕様に沿っており問題ないと判断したときのみ、コミットメッセージを提案する。
- **変更内容の簡潔なサマリ**: コミットメッセージを提案する際は、合わせて以下をユーザーが一目で把握できる形でまとめて報告する。
  - ファイル一覧（変更ファイルと変更行数）
  - 主要な変更点（バグ修正なら原因と修正、機能追加なら追加機能、リファクタなら抽出した責務 等）
  - テスト結果（BE/FE のテスト件数・tsc・既存 Codex 指摘の対応状況）。
    ドキュメントのみの変更などテストが該当しないコミットでは「該当なし」と明記する
